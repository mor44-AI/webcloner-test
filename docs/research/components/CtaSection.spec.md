# CtaSection spec

- **Target file:** `src/components/CtaSection.tsx`
- **Interaction model:** static, with headline letter-by-letter reveal.

## Layout
- Full-bleed background image: `/images/image-bottom-2.avif` (the same portrait as the hero right panel — used elsewhere on the live site too).
- Sky-blue overlay gradient (`rgba(79,128,173,0.15→0.55)`) to ensure white text contrast.
- Centered serif headline (`CTA_HEADLINE` = "Life can be lighter.") + sub headline + "Get early access" pill.
- `section-tuck-lg` rounded bottom corners.
